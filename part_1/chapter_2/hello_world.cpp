#include <iostream>

int main()
{
    /* The << says "write the second argument to the first argument", 
     * in this case, a string to the stdout stream
     */
    std::cout << "hello world\n";
}
