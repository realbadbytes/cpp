#include <iostream>
using namespace std;


int main()
{
    /* types will be automatically inferred. 
     * this can help to avoid long type names, cluttering code. */
    auto b = true;
    auto ch = 'x';
    auto i = 123;
    auto d = 1.2;
    auto z = sqrt(y);

}
